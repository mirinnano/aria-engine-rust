aria;

// Presentation layout belongs to the package's DOM/CSS frontend. This smoke
// story exercises only the semantic screen route.
entry start;

scene start {
  background asset("#07131f");
  wait 1ms;
  end;
}
