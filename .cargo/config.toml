[target.wasm32-unknown-unknown]
# getrandom 0.3 requires an explicit backend for bare wasm. V3's Web adapter
# runs in a browser, so use the platform Web Crypto implementation instead of
# silently compiling an unsupported entropy backend.
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
